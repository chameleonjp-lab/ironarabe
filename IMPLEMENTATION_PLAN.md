# イロナラベ 実装計画 (IMPLEMENTATION_PLAN)

## 実装手順
1. リポジトリ確認（空リポジトリであることを確認済み。既存の index.html なし）。
2. ドキュメント作成: `SPEC.md`, `IMPLEMENTATION_PLAN.md`。
3. `index.html` 単一ファイルにゲーム本体を実装:
   - 3画面（ホーム / ゲーム / 結果）を1ページ内で切り替え（画面の表示/非表示）。
   - 色生成（RGB線形補間による四隅からのグラデーション）。
   - タイル/盤面のデータ構造（タイルID＝正解位置IDで管理）。
   - 固定タイル8マスの配置と見た目の印。
   - 文字列シード → mulberry32 → Fisher-Yates によるシード付きシャッフル（非固定55マスのみ）。
   - タップ交換・選択解除・固定タイル無視。
   - IDによるクリア判定・タイマー停止・操作ロック。
   - タイマー（開始/停止、経過表示 `xx.xxx秒`）と移動回数。
   - 結果画面（タイム・移動回数・ベスト更新有無・シェア）。
4. ローカル保存（プレイヤー名・ベストタイム・最終結果）。
5. ランキング連携（安全分離。設定未登録でも本体が動く）。
6. スマホ表示調整（iPhone SE / iPhone 17 Pro 相当幅）。
7. 最終確認（仕様書との一致、console.log除去、未使用コード除去、README更新）。

## 変更ファイル一覧
- `SPEC.md`（新規）
- `IMPLEMENTATION_PLAN.md`（新規）
- `index.html`（新規・ゲーム本体）
- `README.md`（新規・概要と遊び方）

## 状態管理の設計
グローバルな `state` オブジェクトで管理:
```
state = {
  screen: 'home' | 'game' | 'result',
  solution: number[],      // 位置 index -> 正解タイルID（= その位置のID）
  board: number[],         // 位置 index -> 現在置かれているタイルID
  tiles: {id,targetRow,targetCol,color}[], // タイルID -> タイル定義
  fixedSet: Set<number>,   // 固定位置 index の集合
  selectedIndex: number|null,
  moves: number,
  startTime: number|null,  // performance.now()
  elapsedMs: number,
  cleared: boolean,
  submitted: boolean,      // このプレイでランキング送信済みか
  rafId: number|null,      // タイマー更新のrequestAnimationFrame id
}
```
- 位置 index = row * COLS + col。
- タイルID = そのタイルの正解位置 index。よって `solution[i] === i`。
- クリア判定: すべての i で `board[i] === i`。

## テスト観点
- ロジック: タイルID63個・重複なし・固定8個・固定位置正しい・非固定のみシャッフル・
  IDでクリア判定・クリア後タイマー停止・初期状態が完全正解でない。
- 操作: 1枚目選択・2枚目で交換・再タップで解除・固定タイル選択不可・クリア後操作不可・リタイア可。
- 表示: iPhone SE 幅で横スクロールなし＆盤面が縦にはみ出さない・iPhone 17 Pro 幅で間延びしない・
  経過時間が読める・タイル色が見分けられる・選択中/固定が分かる。
- 保存: プレイヤー名・ベストタイム保存・ベスト更新時だけ更新表示・リロード後も残る。
- ランキング: クリア時のみ送信・リタイア時送信なし・1プレイ1回・送信失敗でも結果画面表示・ミリ秒整数。

## 既知のリスク
- ランキングバックエンド（Supabase等）の実エンドポイント/anon keyが不明。
  → 送信処理を `RANKING_CONFIG` で分離。未設定なら送信をスキップし、結果画面に控えめな失敗表示のみ。
  ゲーム本体は常に動作する。
- `100dvh` 未対応の古い端末 → `vh` フォールバックを併用。
- 色補間の丸め誤差 → クリア判定は色ではなくIDで行うため影響なし。

## 後回しにする機能
複数ステージ / デイリー / ランダム練習 / ヒント / ペナルティ / 音 / 難易度選択 /
多段チュートリアル / ドラッグ / 振動 / 実績 / スキン。ランダム問題のランキング対象化も後回し。
