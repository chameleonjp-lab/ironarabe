# イロナラベ 難易度比較スタディ v1

## 目的

本番の公式seedを変更する前に、3候補を理論値と手動プレイで比較する。

## 本番無変更

この作業では `index.html` を変更していない。現在の公式seedは `ironarabe-official-001-v1` のままで、ランキング条件は変えていない。

## 候補

| 候補 | seed | 位置づけ |
|---|---|---|
| A | `ironarabe-study-easy-074306` | 理論上は現在より少ない交換回数 |
| B | `ironarabe-official-001-v1` | 現在の公式問題そのもの |
| C | `ironarabe-study-hard-000010` | 理論上は現在より多い交換回数 |

## 計算方法

- 盤面は7列×9行、全63枚、固定8枚、可動55枚で計算する。
- 固定位置は `(0,0) (3,0) (6,0) (0,4) (6,4) (0,8) (3,8) (6,8)` とする。
- 色は本番と同じ4隅色からRGB線形補間し、各補間段階で `Math.round()` する。
- seedは文字列を `xfnv1a()` で32ビット整数へ変換し、`mulberry32()`、Fisher-Yatesで固定以外の55枚だけをシャッフルする。
- 理論最少交換回数は、可動55位置だけの `permutation[position] = board[position]` をサイクル分解し、`55 - サイクル数` で計算する。任意の可動タイル2枚を交換できる現在ルールでの数学上の正確な最少交換回数である。ただし、人間が色を見分ける難しさは表さない。
- 理論最少タップ数は `理論最少交換回数 × 2`。選択解除や誤タップは含まない。
- 初期正解数は、固定8枚を除く可動55枚のうち `board[position] === position` を満たす枚数。
- マンハッタン距離は、現在位置とタイルの正解位置について `abs(currentRow - targetRow) + abs(currentCol - targetCol)` を計算する。平均、中央値、最大、1以下、2以下を出す。位置のずれを表す補助値で、色の知覚難易度そのものではない。
- 正解行・正解列は、可動タイルが正解と同じ行または同じ列にある枚数。
- RGB距離は、現在位置の正解色とそこに置かれたタイル色のユークリッド距離 `sqrt((r1-r2)^2 + (g1-g2)^2 + (b1-b2)^2)`。平均、15以下、25以下を出す。人間の知覚に完全には一致しないため、「色の見分けにくさ」を断定する値としては使わない。
- 盤面SHA-256は、63個のタイルIDを位置0から62まで `board.join(',')` したUTF-8文字列で計算する。

## 比較表

`node tools/analyze-difficulty.cjs` の実測値は期待値と一致した。

| 候補 | 理論最少交換 | 理論最少タップ | 初期正解 | サイクル数 | サイクル長（降順） | 平均距離 | 中央 | 最大距離 | 距離1以下 | 距離2以下 | 正解行 | 正解列 | 平均RGB距離 | RGB15以下 | RGB25以下 | SHA-256 |
|---|---:|---:|---:|---:|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| A | 42 | 84 | 4 | 13 | 21,8,7,5,2,2,2,2,2,1,1,1,1 | 4.98 | 5 | 11 | 4 | 12 | 7 | 11 | 77.24 | 4 | 4 | `93976c14fe2a29952a5aee49eba8b7813147fc571b85fea8abaab79a0f25a2ad` |
| B | 49 | 98 | 2 | 6 | 33,12,5,3,1,1 | 5.02 | 5 | 12 | 5 | 12 | 9 | 9 | 78.27 | 2 | 5 | `c8baaed99b3df36a7092faf2c98e330374627726e9668816b476521bf738f362` |
| C | 54 | 108 | 0 | 1 | 55 | 5.31 | 5 | 11 | 3 | 9 | 4 | 9 | 81.24 | 0 | 3 | `d55891fe2531eed9d1985f5105b9cfcce30cb422910dce0e3c3215f1d5877946` |

## 数値から言える範囲

- 候補Aは候補Bより理論最少交換が7回少ない。
- 候補Cは候補Bより理論最少交換が5回多い。
- 候補AとBは初期正解がある。
- 候補Cは可動55枚が1つのサイクルで初期正解が0。
- 平均マンハッタン距離は3候補で大差がない。
- 理論交換回数の差が、そのまま実クリア時間の差になるとは限らない。
- 「候補Aが最も楽しい」などの体感は、この数値だけでは事実として扱わない。

## 比較ラボの安定化

比較ラボは、人間が実際に3候補を遊ぶ前提で次を保証する。

- 画面状態を `home`、`prep`、`countdown`、`play`、`rate`、`summary` に分ける。
- カウントダウンはトークンとタイマーIDで管理し、連打、中断、再挑戦、最初へ戻る操作で古い待機処理を無効化する。
- 計時用の `requestAnimationFrame` は新しい挑戦、中断、クリア時に停止し、複数の計時ループを同時に動かさない。
- 1候補につき結果を1件だけ保存し、全候補終了時は候補A・B・Cの3件が重複なくそろっていることを確認する。
- 挑戦回数は、その候補で盤面表示と計測を開始した回数とする。初回を1とし、再挑戦するたびに1増やす。
- 比較表とコピー結果に挑戦回数を含める。
- Clipboard APIが失敗した場合は一時 `textarea` を試し、`document.execCommand('copy')` が `false`を返した場合は失敗として扱う。
- 色タイルは `button` 要素を使うが、ブラウザ標準の枠、余白、外観を解除する。固定タイルは `disabled` とし、色を薄くしない。
- エラーは `alert()`で止めず、画面内の `aria-live` 領域へ表示する。
- 比較ラボはSupabase通信を行わず、本番の保存キーを読み書きしない。

## 手動比較の注意

- 後に遊ぶ候補ほど、色配置そのものへの学習で有利になる。
- 提示順を並べ替えるのは、この順番の偏りを弱めるため。
- 提示順はページ読込時に1回だけ決まり、「最初へ戻る」では変えない。別順で試す場合はページを再読込する。
- 1人の1回分だけで断定しない。
- タイムだけで選ばない。
- 移動回数、挑戦回数、見分けにくさ、疲れ、もう一度遊びたいかも見る。
- 理論最少交換回数は正確だが、色の見分けやすさは表さない。
- RGB距離は補助値で、人間の知覚難易度そのものではない。
- ブラウザ上の3候補完走は、まだ人間が行う必要がある。
- 自動分析が成功しても、遊びやすさは確定しない。

推奨手順:

1. 3候補を提示順どおり1回ずつ遊ぶ。
2. 各候補の直後に評価を入力する。
3. 一度休む。
4. 必要ならページを再読込し、別の提示順で2回目を行う。
5. 初回タイムだけでなく、移動回数、挑戦回数、疲れも比較する。

## 手動評価欄

| 実施日 | 実施者 | 提示順 | 候補 | タイム | 移動回数 | 挑戦回数 | 見分けにくさ | 疲れ | もう一度遊びたい | メモ |
|---|---|---|---|---:|---:|---:|---:|---:|---:|---|
|  |  |  |  |  |  |  |  |  |  |  |
|  |  |  |  |  |  |  |  |  |  |  |
|  |  |  |  |  |  |  |  |  |  |  |

## 採用判断

公式候補の採用判断は、比較ラボでの手動プレイ後に行う。

## 将来の反映時の注意

公式seedを後で変更する場合は、別作業で次を確認する必要がある。このターンでは実行しない。

- 既存の本番ランキング記録があるか。
- 異なる盤面の記録を同じランキングへ混ぜてよいか。
- `CHALLENGE_ID` を更新するか。
- `BOARD_VERSION` を更新するか。
- `CLIENT_VERSION` を更新するか。
- `ironarabe.v2.official001.*` の保存キーを新しい問題用に分けるか。
- Supabase側の記録をリセットまたは分離する必要があるか。
