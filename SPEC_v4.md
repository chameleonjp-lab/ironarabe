# イロナラベ 仕様書 v4

## 1. 適用範囲

本書は、公開URL `https://chameleonjp.codeberg.page/ironarabe/` で配信する8ステージ版の現行仕様を定める。

## 2. ゲームの核

- 盤面は7列×9行、合計63枚。
- 固定タイルは8枚。
- 動かせるタイル2枚を順番に選んで交換する。
- クリア判定は全タイルIDの位置一致。
- 公式シャッフルseedは `ironarabe-official-001-v1`。
- 固定座標と動かせるタイルのID初期配置は全ステージ共通。
- クリア操作の直後にタイムを止め、完成盤面を4秒表示する。

## 3. ステージ構成

ステージ数は8。2配色系列×4方向で構成する。

### 3-1. 夕映え

```text
#f36b6b
#f0c46a
#5fd0b5
#7b6ff0
```

### 3-2. 極光

```text
#6bf3f3
#6a96f0
#d05f7a
#e4f06f
```

各配色を0°・90°・180°・270°へ時計回りに配置する。90°回転では左上色が右上、右上色が右下、右下色が左下、左下色が左上へ移る。

各ステージは四隅の色からRGB線形補間で63色を生成する。四隅と各辺中央の固定8色は、各ステージ内で重複しない。

## 4. ステージ選択

- 8ステージのインデックスをシャッフルしたバッグを作る。
- 1プレイ開始ごとにバッグ先頭から1件取り出す。
- バッグを使い切るまで同一ステージを再選出しない。
- バッグ再生成時、先頭が直前のステージと同じなら2件目と交換する。
- ページ再読み込み時は新しいバッグとしてよい。
- ステージ選択はタイム計測開始前に完了する。

## 5. ランキング公平性

全ステージで次を共通とする。

- 固定座標8件
- タイルIDと正解位置
- 動かせる55枚の初期ID配列
- 公式シャッフルseed
- 操作方法
- 計時方法
- スコア換算

盤面を解くための交換構造は同一で、ステージ差は色系列と方向だけである。色識別の体感差を監査できるよう、`p_client_version`へステージIDを追加する。

```text
ironarabe-web-1.4.1-stagepack001-v2-sunset-r0
ironarabe-web-1.4.1-stagepack001-v2-aurora-r3
```

## 6. ホーム画面

表示内容:

1. タイトル
2. 配色チップ
3. 説明
4. 全8ステージ・毎回ランダム表示
5. ゲーム開始・遊び方ボタン群
6. 遊び方
7. シェア
8. 実験場リンク

ホーム画面には次を表示しない。

- 端末ローカルベスト
- ベストタイムランキング
- ランキング読込中表示

ページ初期化時とホーム復帰時には、ランキング取得RPCを実行しない。

ボタン余白:

- 開始と遊び方を`.home-actions`へまとめる。
- ボタン間隔は8px。
- 各ボタンの上マージンは0。
- 縦パディングは13px。
- シェアボタンも上マージン0、縦パディング13px。

## 7. ステージ表示

- ホームに「全8ステージ」「毎回ランダム」を表示する。
- カウントダウンにステージ番号・配色名・方向を表示する。
- プレイHUDにステージ番号を表示する。
- 完成表示にステージ番号を表示する。
- 結果画面に配色名・方向・ステージ番号を表示する。
- 結果シェア文へステージ情報を含める。

## 8. タイムと操作

- START後に`performance.now()`で計測開始する。
- 動かせるタイル2枚の交換を1操作とする。
- 正解となる2枚目を選んだ直後にクリア判定する。
- 交換演出時間はクリアタイムへ加えない。
- クリア後は入力を停止する。
- 完成盤面を4000ms表示する。

## 9. 結果画面

結果画面に次を表示する。

- 今回のクリアタイム
- 登録名
- 登録名の初回記録
- 登録名のベスト記録
- 登録名のプレイ回数
- ステージ
- 操作回数
- ベストタイムランキング

登録名の記録は`submit_score`返却値を正本とする。端末ローカルの初回・ベスト・プレイ回数は表示しない。

通信失敗時も今回の結果、再プレイ、シェア、ホーム操作を壊さない。

## 10. ランキング

- `game_slug`: `ironarabe`
- `score_order`: `asc`
- 1秒=100の整数。
- 小数2桁表示。
- `submit_score`本文は4項目だけ。
- `p_client_version`は基本版とステージIDを連結する。
- ランキング取得は`get_best_score_ranking`、最大30件。
- ランキングは結果画面だけに表示する。
- 結果画面遷移時に取得する。
- スコア送信成功後は再取得する。
- 既存記録を削除・分離しない。

## 11. 保存と再プレイ時のキャッシュ

永続保存するゲーム情報はプレイヤー名だけとする。

```text
ironarabe.v2.playerName
```

旧名キー`ironarabe.playerName`は、現行名キーがない場合だけ移行に利用する。

「もう一度遊ぶ」を押した時は、保存名を退避した上で次を実行する。

- `ironarabe.`で始まるlocalStorageを、現行プレイヤー名キー以外削除
- `ironarabe.`で始まるsessionStorageを削除
- 保持中のタイル、正解配列、盤面配列、固定集合を解放
- ランキング配列と結果ランキングDOMを消去
- 古い非同期結果を無効化するため`playId`と`rankingRequestId`を更新
- 登録名表示と結果表示を初期化

ステージのシャッフルバッグと直前ステージ番号は、8ステージの重複防止を維持するためページ内では保持してよい。

Supabaseの記録は削除しない。

## 12. バージョン

```text
GAME_SLUG: ironarabe
CHALLENGE_ID: ironarabe-official-001
BOARD_VERSION: 2
CLIENT_VERSION: ironarabe-web-1.4.1-stagepack001-v2
SHUFFLE_SEED: ironarabe-official-001-v1
```

## 13. 受け入れ条件

- ステージが正確に8件生成される。
- 2配色それぞれに0°・90°・180°・270°がある。
- 各ステージの固定8色が重複しない。
- 全ステージのタイルID初期配列が同一である。
- 同一バッグ内でステージが重複しない。
- バッグ境界で同じステージが連続しない。
- ホーム画面にランキングと端末ベストが存在しない。
- 初期表示とホーム復帰でランキング通信を開始しない。
- 結果画面に登録名のベスト記録とランキングがある。
- 再プレイで保存名以外のアプリキャッシュを削除する。
- 既存の8ステージ、4秒完成表示、ランキング送信契約を維持する。
