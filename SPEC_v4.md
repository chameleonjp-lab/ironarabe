# イロナラベ 仕様書 v4

## 1. 適用範囲

本書は、公開URL `https://chameleonjp.codeberg.page/ironarabe/` で配信する8ステージ版の仕様を定める。

## 2. ゲームの核

- 盤面は7列×9行、合計63枚。
- 固定タイルは8枚。
- 動かせるタイル2枚を順番に選んで交換する。
- クリア判定は全タイルIDの位置一致。
- 公式シャッフルseedは `ironarabe-official-001-v1`。
- 固定座標と動かせるタイルのID初期配置は全ステージ共通。
- クリア操作の直後にタイムを止め、完成盤面を4秒表示する。

## 3. ステージ構成

ステージ数は8。2配色系列×4方向で構成する。

### 3-1. 夕映え

時計回りの四隅の基準色:

```text
#f36b6b
#f0c46a
#5fd0b5
#7b6ff0
```

方向:

```text
0°
90°
180°
270°
```

### 3-2. 極光

時計回りの四隅の基準色:

```text
#6bf3f3
#6a96f0
#d05f7a
#e4f06f
```

方向:

```text
0°
90°
180°
270°
```

### 3-3. 色生成

各ステージは四隅の色からRGB線形補間で63色を生成する。固定8座標は、四隅と各辺中央に位置する。8固定色は各ステージ内で重複しない。

### 3-4. 時計回り回転

角色の時計回り90°回転は次の位置移動とする。

```text
左上 → 右上
右上 → 右下
右下 → 左下
左下 → 左上
```

180°・270°は同じ変換を繰り返す。

## 4. ステージ選択

- 8ステージのインデックスをシャッフルしたバッグを作る。
- 1プレイ開始ごとにバッグ先頭から1件取り出す。
- バッグを使い切るまで同一ステージを再選出しない。
- バッグ再生成時、先頭が直前のステージと同じなら2件目と交換する。
- ページ再読み込み時は新しいバッグとしてよい。
- ステージ選択はタイム計測開始前に完了する。

## 5. ランキング公平性

全ステージで次を共通とする。

- 固定座標8件
- タイルIDと正解位置
- 動かせる55枚の初期ID配列
- 公式シャッフルseed
- 操作方法
- 計時方法
- スコア換算

したがって、盤面を解くための交換構造は同一であり、ステージ差は色系列と方向だけである。

ただし、人による色の識別難易度には差が生じ得る。ランキングは既存記録と共通のまま維持し、送信する`p_client_version`へステージIDを追加して後からステージ別に監査できるようにする。

例:

```text
ironarabe-web-1.4.0-stagepack001-v2-sunset-r0
ironarabe-web-1.4.0-stagepack001-v2-aurora-r3
```

## 6. ホーム画面

表示順:

1. タイトル
2. 配色チップ
3. 説明
4. 全8ステージ・毎回ランダム表示
5. ゲーム開始・遊び方ボタン群
6. 遊び方
7. 端末ローカルベスト
8. シェア
9. 実験場リンク
10. ランキング

ボタン余白:

- 開始と遊び方を`.home-actions`へまとめる。
- ボタン間隔は8px。
- 各ボタンの上マージンは0。
- 縦パディングは13px。
- シェアボタンも上マージン0、縦パディング13px。

一般の`.btn`や結果画面のボタンは、この調整によって一括変更しない。

## 7. ステージ表示

- ホームに「全8ステージ」「毎回ランダム」を表示する。
- カウントダウンにステージ番号・配色名・方向を表示する。
- プレイHUDにステージ番号を表示する。
- 完成表示にステージ番号を表示する。
- 結果画面に配色名・方向・ステージ番号を表示する。
- 結果シェア文へステージ情報を含める。

## 8. タイムと操作

- START後に`performance.now()`で計測開始する。
- 動かせるタイル2枚の交換を1操作とする。
- 正解となる2枚目を選んだ直後にクリア判定する。
- 交換演出時間はクリアタイムへ加えない。
- クリア後は入力を停止する。
- 完成盤面を4000ms表示する。

## 9. ランキング

- `game_slug`: `ironarabe`
- `score_order`: `asc`
- 1秒=100の整数。
- 小数2桁表示。
- `submit_score`本文は4項目だけ。
- `p_client_version`は基本版とステージIDを連結する。
- ランキング取得は`get_best_score_ranking`、最大30件。
- 既存の1ステージ版記録を削除・分離しない。

## 10. ローカル保存

最後の結果へ次を保存する。

```text
playerName
elapsedMs
score
operations
challengeId
boardVersion
stageId
stageLabel
clientVersion
at
```

名前、端末初回、端末ベスト、端末プレイ回数の既存キーは維持する。

## 11. バージョン

```text
GAME_SLUG: ironarabe
CHALLENGE_ID: ironarabe-official-001
BOARD_VERSION: 2
CLIENT_VERSION: ironarabe-web-1.4.0-stagepack001-v2
SHUFFLE_SEED: ironarabe-official-001-v1
```

## 12. 受け入れ条件

- ステージが正確に8件生成される。
- 2配色それぞれに0°・90°・180°・270°がある。
- 各ステージの固定8色が重複しない。
- 全ステージのタイルID初期配列が同一である。
- 同一バッグ内でステージが重複しない。
- バッグ境界で同じステージが連続しない。
- ホームボタンだけ余白が整い、結果ボタン等を壊さない。
- ステージ情報が各画面とシェア文へ表示される。
- 既存ランキング・名前記録・4秒完成表示が維持される。
- iPhone SE級幅でHUDと結果ステージ表示が重ならない。
