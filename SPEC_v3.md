# イロナラベ 仕様書 v3

## 1. 適用範囲

本書は、公開URL `https://chameleonjp.codeberg.page/ironarabe/` で配信する公式チャレンジの現行仕様を定める。

## 2. 変更しないゲームの核

- 盤面は7列×9行、合計63枚。
- 固定タイルは8枚。
- 動かせるタイル2枚を順番に選択して交換する。
- 公式seedは `ironarabe-official-001-v1`。
- クリア判定は色比較ではなく全タイルIDの位置一致。
- ランキングは公式問題だけを対象とする。

## 3. 画面状態

```text
home
nameConfirm
countdown
playing
result
```

クリア時は `playing` のままタイムと入力を停止し、完成した盤面を4秒表示してから `result` へ遷移する。

## 4. ホーム

- タイトル、説明、ゲーム開始、遊び方、ローカルベスト、シェア、実験場リンクを表示する。
- 最下部にベストタイムランキングを表示する。
- ランキングは30件取得する。
- 表示領域は5行分を基本高さとし、領域内の縦スクロールで30位まで閲覧できる。
- 記録が0件の場合は「まだランキング記録がありません。」と表示する。
- 通信失敗はランキング領域だけへ表示し、ゲーム開始を妨げない。

## 5. 名前確認

- 前後空白除去後1〜20文字。
- 絵文字と記号を許可する。
- 保存済みの名前を初期値にする。
- 名前確定時に入力フォーカスを外す。
- 空欄や上限超過では開始しない。

## 6. カウントダウン

- `3 → 2 → 1 → START`。
- カウントダウン中は盤面を表示しない。
- START前に計測しない。
- 連打で複数のカウントダウンを起動しない。

## 7. プレイと操作回数

- 「操作回数」は、動かせるタイル2枚の交換が1回完了するたびに1増える。
- 1枚目の選択、選択解除、固定タイルへのタップは操作回数に含めない。
- 交換反応中は次の入力を受け付けない。
- 旧表記「移動回数」は使用しない。

## 8. タイムと完成表示

- 記録の正本は `performance.now()` の開始時刻との差。
- 全ID一致を検出した時点で、入力をロックし、`requestAnimationFrame`を止める。
- その直後に最終経過時間を1回だけ確定する。
- 確定後にタイムを再計算しない。
- 完成盤面と「完成！」表示を4000ms維持する。
- 完成表示中にランキング送信を開始してよい。
- 4000ms後に同じ`playId`であることを確認して結果画面へ進む。

## 9. 結果画面

表示内容:

- 今回のクリアタイム
- 操作回数
- ランキング送信状態
- この名前の記録
  - プレイヤー名
  - 初回記録
  - ベスト記録
  - プレイ回数
- ベストタイムランキング
- 再挑戦、結果シェア、ホーム、実験場への導線

結果画面に「この端末のプレイ回数」は表示しない。

名前記録は `submit_score` の返却値を使用する。

```text
result_display_name
result_first_score
result_best_score
result_play_count
is_first_play
is_new_best
```

通信失敗時は名前記録を取得できなかったことを表示し、結果・再挑戦・シェア・ホーム操作を壊さない。

## 10. ランキング表示

取得RPC:

```text
get_best_score_ranking
```

本文:

```json
{
  "p_game_slug": "ironarabe",
  "p_limit": 30
}
```

表示項目:

- `rank_no`
- `display_name`
- `best_score`を秒・小数2桁へ変換した値

同率順位はRPCが返した`rank_no`をそのまま表示する。

ホームと結果画面は同じ取得結果を共有できる。クリア送信成功後はランキングを再取得する。

## 11. スコア送信

送信RPC:

```text
submit_score
```

本文は次の4項目だけ。

```text
p_display_name
p_game_slug
p_score
p_client_version
```

- `p_score = Math.round(elapsedMs / 10)`。
- 1秒=100。
- クリア1回につき送信1回。
- リタイアでは送信しない。
- Publishable keyは`apikey`ヘッダーだけに使用する。
- Authorization Bearer、secret、service_roleは使用しない。
- タイムアウトは10秒。

## 12. 版

```text
GAME_SLUG: ironarabe
CHALLENGE_ID: ironarabe-official-001
BOARD_VERSION: 2
CLIENT_VERSION: ironarabe-web-1.3.0-official001-v2
```

## 13. ローカル保存

ローカルには名前、初回、ベスト、端末内プレイ回数、最後の結果を保存してよい。

ただし、結果画面の公式な「この名前の記録」はSupabase返却値を使用し、端末内記録と混同しない。ホームのローカルベストには端末記録であることを明記する。

## 14. Supabase登録

2026年7月20日に有効化済み。

```text
is_active: true
release_date: 2026-07-20
display_order: 32
top_ranking_type: best
score_order: asc
score_unit: 秒
score_scale: 100
score_decimals: 2
```

## 15. 受け入れ条件

- 7×9、固定8枚、seed、色、ID判定を変更していない。
- クリア時点でタイムが停止する。
- 完成盤面を4秒表示する。
- ホームと結果にランキングがある。
- 30件取得し、5行表示領域内でスクロールできる。
- 結果のプレイ回数は端末値ではなく名前に紐づくSupabase値である。
- 全ユーザー向け表記が「操作回数」で統一される。
- 送信・取得失敗でゲーム結果が壊れない。
- 二重送信しない。
