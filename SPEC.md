# イロナラベ 仕様書 (SPEC)

## ゲーム概要
『イロナラベ』は、色のグラデーションタイルを正しい位置に並べ替えるタイムアタックパズルです。
7列 × 9行（合計63マス）の盤面に、四隅の色から補間して生成したグラデーションタイルを配置します。
シャッフルされたタイルを2つ選んで入れ替え、すべてを正しいグラデーション位置に戻すとクリアです。
クリアまでのタイム（ミリ秒）がスコアで、小さいほど良い成績です。

初回バージョンでは、独自ルールを一切入れず「色タイルを正しい位置に戻す」基本パズルとして成立させることを最優先とします。

## 操作方法
- タップ操作のみで成立します。ドラッグ・スワイプは使いません。
- 動かせるタイルを1つタップすると選択状態になります。
- もう1つの動かせるタイルをタップすると、2つのタイルが入れ替わります。
- 選択中のタイルをもう一度タップすると選択解除されます。
- 固定タイル（印付き）はタップしても選択されず、入れ替えもできません。
- 交換のたびに移動回数が1増え、クリア判定が行われます。
- クリア後は盤面操作を受け付けません。

## 盤面仕様
- 列数: 7 (col: 0〜6)
- 行数: 9 (row: 0〜8)
- 合計: 63マス
- 固定タイル: 8マス
  - (col,row) = (0,0) (3,0) (6,0) / (0,4) (6,4) / (0,8) (3,8) (6,8)
  - 左上・上辺中央・右上 / 左辺中央・右辺中央 / 左下・下辺中央・右下
- 固定タイルは常に正解位置に置かれ、選択・交換ともに不可。
- クリア条件: 全63マスについて「現在置かれているタイルID === その位置の正解ID」。

## 色生成仕様
四隅の色を線形補間（RGB）して各マスの色を生成します。

公式チャレンジ:
```
id:          'ironarabe-official-001'
topLeft:     '#f36b6b'
topRight:    '#f0c46a'
bottomLeft:  '#7b6ff0'
bottomRight: '#5fd0b5'
```

各マスの計算（cols=7, rows=9）:
```
x = col / (cols - 1)
y = row / (rows - 1)
top    = lerp(topLeft, topRight, x)
bottom = lerp(bottomLeft, bottomRight, x)
color  = lerp(top, bottom, y)
```

- 色の一致では判定しません。丸め誤差を避けるため、正解位置は必ずタイルIDで管理します。
- 各タイル: `{ id, targetRow, targetCol, color }`（id = targetRow * cols + targetCol）。
- 各盤面位置: 現在そこに置かれているタイルIDを保持。

## シャッフル仕様
- 固定タイルを除く55マスのタイルIDのみをシャッフルします。
- 固定タイルは最初から正解位置に固定。
- 固定シードの疑似乱数を使用し、同じ端末・別端末を問わず初期配置が同一になります。
  - シード: `ironarabe-official-001-v1`
  - 文字列シード → 数値化（xfnv1a）→ mulberry32 でPRNG生成 → Fisher-Yates。
- 保証事項:
  - 同じブラウザ・別端末で初期配置が同じ。
  - 固定タイルは動かない。
  - 完全正解済み状態では始まらない（万一一致した場合は再シャッフル）。
  - シャッフル後にタイル数が増減しない／IDが重複しない。

## クリア条件
すべての位置 i について `board[i] === solutionId[i]` であればクリア。

## スコア仕様
- スコア = クリアタイム。小さいほど良い。
- タイマーはスタート押下でゲーム画面に入った時点から開始。
- 内部スコアはミリ秒整数（例: 12.345秒 → 12345）。
- 表示形式: `12.345秒`（秒を小数3桁）。
- ペナルティ・ヒントは初回バージョンでは無し。

## ランキング仕様
- game_slug: `ironarabe`
- title: イロナラベ
- top_ranking_type: best
- score_order: asc（小さいほど上位）
- score_unit: 秒
- score_scale: 1000
- score_decimals: 3
- score_label: クリアタイム
- first_score_label: 初回タイム
- best_score_label: ベストタイム
- スコアはミリ秒整数で送信。
- クリア時のみ送信。リタイア時は送信しない。1プレイにつき送信は1回だけ。
- 送信失敗してもゲーム画面・結果画面は壊れない。
- Supabaseの secret key は使わない。公開可能なキー（anon key）のみ。
- 送信設定が未登録／未設定の場合はスキップし、結果画面に小さく「ランキング送信に失敗しました」と表示する程度に留める。
- ランキング送信処理はゲーム本体から分離し、失敗が本体に波及しないようにする。

## ローカル保存仕様
localStorage を使用（キーは他ゲームと衝突しないよう `ironarabe.` プレフィックス）:
- `ironarabe.playerName` : プレイヤー名
- `ironarabe.bestTimeMs` : ローカルベストタイム（ミリ秒整数）
- `ironarabe.lastResult` : 最終プレイ結果（JSON）

## UI条件
- スマホ縦持ちを基準。iPhone SE 幅でも盤面が画面内に収まる。
- 横スクロールを出さない。ダブルタップ拡大・長押しメニューを抑制。
- 盤面は画面高さに応じて自動で縮む（CSSで `min(...)` を用いてサイズ制御）。
- タップ領域を小さくしすぎない。文字を小さくしすぎない。
- 選択中タイルは分かりやすく強調。固定タイルには小さな印。
- 結果画面には必ずホームへ戻るボタン。シェア文には必ずURLを含める。
- クリア演出は派手すぎない。

## 今回入れない機能
複数ステージ / デイリーチャレンジ / ランダム練習モード / ヒント / 誤交換ペナルティ /
BGM / 効果音 / アニメーション強化 / 難易度選択 / 多段チュートリアル / タイルドラッグ /
スマホ振動 / 実績 / スキン変更。
また、ランダム問題はランキング対象にしない。
